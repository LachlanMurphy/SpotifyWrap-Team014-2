<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Favorites Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style_search.css">
</head>
<body>            
    <h1 style="text-align: center; color: #ffffff;"> {{user.name}}'s favorite songs:</h1>
    {{#each data}}
        <tr>
            <td>
                <div class="container">
                    <div class="card favorites-song-result mb-3 p-3">
                        <div class="d-flex align-items-center">
                            <!-- Album Cover -->
                            <div class="album-cover me-3">
                                <a href="{{this.uri}}">
                                    <img src="{{this.album_url}}" alt="Album Cover" class="rounded">
                                </a>
                            </div>
                            
                            <!-- Song Details (Stacked Vertically) -->
                            <div class="song-info flex-grow-1">
                                <h1 class="song-name mb-1">{{this.song_name}}</h1>
                                <h5 class="artist-name mb-1">{{this.artist_name}}</h5>
                                <p class="album-title mb-0">{{this.album_name}}</p>
                            </div>
                            
                            <!-- Duration -->
                            <div class="duration ms-3 text-muted">
                                <span>Duration: {{this.song_duration}}</span>
                            </div>               
                        </div>
                    </div>
                </div>
            </td>
        </tr>
    {{/each}}
</body>

</html>