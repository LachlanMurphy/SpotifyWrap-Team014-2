<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212; 
            color: #b3b3b3;
            min-height: 100vh;
            margin: 0;
        } 
        .container {
            display: flex;
            height: 100vh;
            width: 100vw;
        }
        .sidebar {
            width: 100px;
            background-color: #333;
            color: white;
            padding: 20px;
            display: flex;
            align-items: start;
        }
        .main-content {
            flex-grow: 1;
            padding: 20px;
            display: flex;
            align-items: start;
            justify-content: center;
        }
        h5, h2, h1 {
            color: #ffffff; 
            text-align: center;
        }
        .card {
            width: 45rem; 
            height: 18rem; 
            background-color: #181818;
            border: 2px solid #1DB954;
            border-radius: 10px;
            color: #b3b3b3;
            box-shadow: 0 0 10px 5px rgba(29, 185, 84, 0.5);
        }
        .card:hover {
            box-shadow: 0 0 20px 10px rgba(29, 185, 84, 0.7);
        }
        .btn-primary {
            background-color: #1DB954; 
            border-color: #146831;
        }
        .btn-primary:hover {
            background-color: #1DB954;
            border-color: #146831;
        }
        .card-text {
            color: #b3b3b3;
        }
        .card-container {
            padding-top: 15vh;
            display: flex;
            justify-content: center;
        }
        .song-result {
            background-color: #181818;
            color: #b3b3b3;
            border: none;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        }
        .album-cover img {
            width: 60px;
            height: 60px;
            object-fit: cover;
        }
        .song-name {
            color: #ffffff;
            font-size: 1rem;
        }
        .album-title {
            color: #b3b3b3;
            font-size: 0.9rem;
        }
        .duration {
            font-size: 0.9rem;
            color: #b3b3b3;
        }
        .song-info {
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
    </style>
</head>
<body>
    <h1 style="text-align: center; color: #ffffff;">Search for Songs</h1>
    <input type="text" class="search-bar" placeholder="Search..">
    <div class="container mt-4">
    <div class="card song-result mb-3 p-3">
        <div class="d-flex align-items-center">
            <!-- Album Cover -->
            <div class="album-cover me-3">
                <img src="https://via.placeholder.com/60" alt="Album Cover" class="rounded">
            </div>
            
            <!-- Song Details (Stacked Vertically) -->
            <div class="song-info flex-grow-1">
                <h5 class="song-name mb-1">Song Name</h5>
                <p class="album-title mb-0">Album Title</p>
            </div>
            
            <!-- Duration -->
            <div class="duration ms-3 text-muted">
                <span>3:45</span>
            </div>
        </div>
    </div>
</div>

</body>
</html>
